package clases;

import interfaces.iAddress;

public class SpainAddress extends AddressBook implements iAddress {
    public SpainAddress(String name, String address) {
        super(name, address);
    }
}
